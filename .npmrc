# @deepseek-ai/* are provided by the DSH runtime; link them from the host checkout for type resolution.
# pnpm 9 uses camelCase keys (auto-install-peers is ignored → install kept defaulting to true and
# tried to fetch @deepseek-ai/* from the registry). Keep the dashed alias too for older pnpm.
autoInstallPeers=false
auto-install-peers=false
strict-peer-dependencies=false
# Deps are host-provided (see above); skip pnpm's pre-run install/verify so `pnpm run` works offline.
verify-deps-before-run=false
